
function countItems(){
    // var items = options.length;
    // console.log(items)
    // debugger
    // var itemList = options.slice(0)
    // console.log(itemList)
    for(var i = 0; i <= options.length; i++){
        var options = document.getElementsByTagName("option")[0]
        console.log(options.innerHTML)

    }
    // alert(" Number of Items : " + items  + itemList  )
}